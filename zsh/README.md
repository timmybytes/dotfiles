# zsh & ohmyzsh

My settings for using `zsh` with `oh-my-zsh` on macOS. To use with your local
setup, create a `.env` file in the same directory you keep your `.zshrc`. In
your `.env`, add:

```
USER=/path/to/your/local/user
```

For example, `USER=/Users/Bob` on macOS.
